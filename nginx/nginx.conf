server {
    listen 80;

    location /site1/ {
        proxy_pass http://site1/;
        proxy_set_header Host $host;
    }

    location /site2/ {
        proxy_pass http://site2/;
        proxy_set_header Host $host;
    }
}